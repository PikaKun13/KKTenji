// deck 作成用プロンプト雛形（ヘルプ「deck の作り方」からコピーして AI 対話に貼る。設計書 §5）
// 自己完結であること: この文章だけで第三者の AI が正しい sidecar を出力できる必要がある。
export const PROMPT_TEMPLATE = `あなたは deck（プレゼン資料）を KKTenji 用の関係図定義 sidecar（*.tenji.json）に変換するアシスタントです。
これから私が資料の内容を渡すので、以下の仕様に従って JSON を 1 つだけ出力してください。

## 入力の渡し方（私がやること）
- PowerPoint の場合: 各ページのタイトルと本文テキストを「--- ページ N ---」区切りで貼ります
- Markdown の場合: ファイル全文を貼ります（## 見出し または --- がページ区切り）

## 出力仕様: deck.tenji.json（v1）
\`\`\`
{
  "version": 1,
  "title": "deck のタイトル",
  "source": { "type": "pptx" | "md", "path": "deck.pptx などファイル名のみ", "pageBy": "h2" },
  "nodes": [
    { "id": "英数字の短い一意 id", "title": "ノード見出し(≤20字)", "parent": "親の id | null",
      "page": ページ番号(1始まり) | null, "summary": "一行要旨(任意)" }
  ],
  "links": [
    { "from": "id", "to": "id", "type": "support|echo|contrast|cause",
      "direction": "->" | "<->", "label": "短い説明(任意)" }
  ],
  "flow": ["プレゼンで停止する頁ノードの id を頁順に"]
}
\`\`\`
- source.path はコンテンツファイル名のみ（パス区切りや .. を含めない）
- pageBy は md のときのみ（"h2" = ## 見出し区切り / "hr" = --- 区切り）

## 関係線 4 種の使い分け
- support（支撑）: A が B の主張を支える・根拠になる
- echo（呼応）: 離れた頁が同じテーマを再訪する
- contrast（対比）: 対立・比較して見せたい
- cause（因果）: A の結果として B が起こる

## 樹の組み方（重要）
1. 全ノードを第 2 層に平坦に並べるのは禁止。章 → 節 → 頁の 3〜4 層で組む
2. 章ノード（page: null の構造ノード）で意味の塊を作り、実ページはその配下へ
3. 1 つの親の直下に 15 ノード以上ぶら下げない
4. links は「離れた頁の間の意味的関係」に絞る（多くて頁数の 1/3 程度。隣接頁を機械的に繋がない）
5. flow は page を持つノードのみ・元資料の頁順どおりに全頁を並べる（章ノードは入れない）

## 自己検証してから出力すること
- すべての id が一意で、parent / links / flow の参照先が実在する
- 頁を持つ全ノードの page が 1..総頁数 の範囲で重複しない
- JSON として構文が正しい（コメント・末尾カンマ禁止）
- （リポジトリの schema/tenji-v1.schema.json があれば、それに適合する）

出力はコードブロックに入れた JSON 本体のみ。保存先はコンテンツファイルと同じフォルダに
「<コンテンツファイル名から拡張子を除いたもの>.tenji.json」（例: deck.pptx → deck.tenji.json）。

準備ができたら「資料をどうぞ」と返してください。`;
